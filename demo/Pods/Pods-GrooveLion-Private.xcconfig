#include "Pods-GrooveLion.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/GrooveLion" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/GrooveLion"
OTHER_LDFLAGS = -ObjC ${PODS_GROOVELION_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}